<%= form_for(@type, url: @path, html: { method: @method }) do |f| %>
  <% if @type.errors.any? %>
    <div class="error_messages">
      <h3>Submission failed</h3>
      <ul>
        <% for message in @type.errors.full_messages %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title, "Title" %>
    <%= f.text_field :title, value: restore_whitespace(@cluster.title) %>
  </div>

  <div>
    <%= f.label :description, "Description" %>
    <%= f.text_field :description %>
  </div>
  
  <div>
    <%= f.submit "Submit" %>
  </div>
  
<% end %>