<h1>Member::Publications#index</h1>
<table>
  <% @publications.each do |article| %>
    <tr>
      <td><%= link_to article.title, administration.publication_path(article.id) %></td>
    </tr>
  <% end %>
</table>